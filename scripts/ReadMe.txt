For checking the chokidar, following files are used for running it : -
1. WatchingDirectory.js(Run this file and uncomment the commented uploadToS3 code).
2. UploadToS3.js

For Checking the directory watcher, following files are used for running it :-
1. MainFile2.js (Run this file).
2. Repository2.js (This file contains the sqlite method for creation, insertion & selection).
3. Node_dir_Example (It consists of the "Path" module methods to get all the paths of sub-folders and files).

Note : - Don't consider files MainFile.js, Repository.js and FileOperation.js files.
